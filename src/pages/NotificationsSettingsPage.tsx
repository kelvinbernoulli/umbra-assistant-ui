export default function NotificationsSettingsPage() {
  return <div className="empty-state"><strong>Notifications are unavailable</strong><span>The server does not provide notification delivery or scheduling yet.</span></div>
}
